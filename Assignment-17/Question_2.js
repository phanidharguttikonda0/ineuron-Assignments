/*
    Iterate over all the characters of the word “iNeuron”.
*/

word = 'iNeuron' ;

for(x in word)
    console.log(word[x]) ;
// we can use any either of these methods 
for(x of word)
    console.log(x)

var word ;